{% extends 'base.html.twig' %}

{% block title %}AppelOffre{% endblock %}

{% block body %}
    <h1>AppelOffre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ appel_offre.id }}</td>
            </tr>
            <tr>
                <th>Contexte</th>
                <td>{{ appel_offre.contexte }}</td>
            </tr>
            <tr>
                <th>DateExp</th>
                <td>{{ appel_offre.dateExp ? appel_offre.dateExp|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_appel_offre_index') }}">back to list</a>

    <a href="{{ path('app_appel_offre_edit', {'id': appel_offre.id}) }}">edit</a>

    {{ include('appel_offre/_delete_form.html.twig') }}
{% endblock %}
